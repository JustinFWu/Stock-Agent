  --validate (technical only)
  
  ┌─────────────────────────────────┬──────────┬───────────────┐  │                                 │ Mean AUC │ Mean Accuracy │
  ├─────────────────────────────────┼──────────┼───────────────┤  │ Walk-forward (2 splits)         │ 0.496    │ 0.503         │
  ├─────────────────────────────────┼──────────┼───────────────┤
  │ Holdout (never-trained tickers) │ 0.505    │ —             │
  └─────────────────────────────────┴──────────┴───────────────┘

  Per fold: fold 1 AUC 0.491, fold 2 AUC 0.501 (walk-forward); holdout 0.499 / 0.511.  Essentially coin-flip — consistent with your saved note that sub‑0.5 AUC is
  structural here, and the pooled model lands right at ~0.50.

  Now, the --news run — this is the decision I need from you. Network is back, so it
  can run, but it's not free:

  - Only 23/71 news frames are cached. The other 48 must be rebuilt from scratch —
  fetch headlines + score each through Claude Haiku. Those 48 tickers were never
  successfully scored (they got 0 news during the DNS outage), so their headlines are  not in sentiment_cache.json — these are fresh API calls.
  - At the ~13 min/ticker you saw during the original run, that's roughly ~10 hours
  and real (if modest, Haiku) API cost, plus the ~9k new headlines added to the
  cache.
  - I can't meaningfully compare tech-vs-news until those 48 are built — if I run it
  now without rebuilding, my new fail-safe code would just skip any that hiccup,
  giving a lopsided 23-ticker comparison.