  Dies as part of Phase 2 (removing now would break Phase 1, which rides on the binary model):
  up_next_week as the training target, walk_forward_validate's AUC/accuracy view, the
  CONFIDENCE_THRESHOLD no-trade zone, and predict.py's UP/DOWN/NO_TRADE — which should become
  "here's this ticker's cross-sectional rank in today's universe," not a standalone absolute call.  Keep add_label/future_return through all of it.
  [DONE — all of the above happened in the Phase 2 commit.]

================================================================================
CURRENT STATE OF THE PROJECT  (2026-07-27)
================================================================================

VERDICT: Cross-sectional ranking was built and tested end-to-end. It is a NO-GO.
The current feature set has no usable predictive signal. The alpha hunt is paused
here on purpose — the cheap, high-value test came back negative before any
execution/risk/webapp layer was built on top of it.

WHAT THE MODEL IS NOW
  - One pooled cross-sectional RANKING model (XGBRegressor) trained on
    xs_demeaned_return = each name's 5-day forward return minus the universe mean
    that day. It learns to ORDER names within a date, not call absolute up/down.
  - Validation is rank-based: `python pipeline.py --rank-validate` reports per-date
    rank IC, IC info ratio, and a top/bottom-quantile long/short spread.
    `--validate` adds the never-trained-ticker holdout (also rank IC).

THE PRE-COMMITTED GATE:  mean rank IC > ~0.03, stable across folds, L/S Sharpe > ~1.

RESULTS (all fail the gate; universe = 71 train tickers, ~2y daily):
  Phase 1  5-day, ranked the OLD binary classifier's up-prob :  IC +0.0037
  Phase 2  5-day, proper regressor on xs_demeaned_return      :  IC -0.0003
  Horizon  21-day (monthly), proper regressor                 :  IC -0.0185
  News     5-day, proper regressor WITH news features         :  IC -0.0005
  (Earlier: absolute single-name AUC was ~chance too.)
  Folds always disagree in sign and cancel -> noise, not edge. Well-powered
  (~312 date-days x ~71 names). A synthetic control with a planted signal
  recovered IC ~0.49, so the test harness detects signal when it exists.

PER-FEATURE DIAGNOSTIC (in-sample, whole 2y = optimistic upper bound):
  NO feature clears 0.03. Best is atr_pct at IC 0.0255 (IR 0.09 — noise-level).
  news_sentiment is the 5th-strongest raw feature (|IC| 0.019) BUT the sign is
  NEGATIVE (contrarian: higher sentiment -> slightly lower forward return) and
  unstable (IR -0.15). So news isn't dead weight, but it's weak, contrarian, and
  sub-gate. With everything at noise level, the model has nothing stable to learn.

WHAT'S THE ONLY LEVER LEFT
  Genuinely NEW, orthogonal INPUTS (real point-in-time fundamentals, analyst-estimate
  revisions, alt-data). That is weeks of pipeline work with real leakage risk — do it
  ONLY with specific conviction about a data source, not to avoid the negative. Do NOT
  run Phase 3 (rank:pairwise/ndcg): it re-weights ordering of the same zero-IC features
  and won't manufacture edge ("one more tweak" trap).
  One specific thread if revisited: build a proper event-time / surprise-relative news
  dataset (not daily-averaged) and test news_sentiment as a MEAN-REVERSION signal.

CODE STATE (ranking-native)
  - target.py: add_cross_sectional_labels() adds xs_demeaned_return / xs_rank_pct /
    xs_top_tercile on the POOLED frame (within-date only, no leakage). add_label /
    future_return kept as the substrate. up_next_week no longer used by the model.
  - dataset.py: build_pooled_dataset computes the xs labels after concat.
  - train.py: LABEL_COL = xs_demeaned_return; _build_model = XGBRegressor;
    _walk_forward_folds yields a continuous score; cross_sectional_validate is the gate;
    walk_forward_holdout now reports rank IC. Deleted: walk_forward_backtest and the AUC
    walk_forward_validate.
  - pipeline.py: --rank-validate (+ --quantile); --validate and API /validate now run
    the ranking validation.
  - predict.py / api /predict: single-ticker UP/DOWN is RETIRED (raises NotImplementedError)
    — meaningless for a ranker. A universe-ranking prediction path was never built (gated
    on the gate, which failed).
  - config.py: FORWARD_DAYS back to 5.
